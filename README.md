# fast-agent card packs

Registry and reference card packs for `fast-agent`.

## Available packs

- `smart` — a minimal single-card test pack.

## Install

```bash
fast-agent cards registry https://github.com/fast-agent-ai/card-packs
fast-agent cards add smart
```
